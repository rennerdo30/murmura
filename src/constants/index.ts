export const BURNING_THRESHOLD = 50;
export const MAX_BURNING_INTENSITY_STREAK = 100;

/** localStorage keys. Shared so the pre-paint theme script cannot drift. */
export const SETTINGS_STORAGE_KEY = 'murmura_settings';
export const TARGET_LANGUAGE_STORAGE_KEY = 'murmura_target_language';

/** Theme applied before any user preference is known. */
export const DEFAULT_THEME = 'ja';
